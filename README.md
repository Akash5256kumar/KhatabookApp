# apna_business_app

## API setup

The app talks to the FastAPI backend over `BASE_URL`.

Default:

- `http://168.144.112.102`

Example override:

```bash
flutter run --dart-define=BASE_URL=http://168.144.112.102
```
